#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int x,y,product;
    scanf("%d", &x);
    scanf("%d", &y);
    product=x*y;
    printf("%d",product);
    return 0;
}